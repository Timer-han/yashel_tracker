# 🕌 Яшел Трекер - Трекер пропущенных намазов

Telegram-бот для отслеживания и восполнения пропущенных намазов с использованием Python, aiogram и SQLite.

## 🌟 Функционал

### Для пользователей:
- 📝 **Регистрация с детальным сбором данных**
  - Базовая информация: пол, дата рождения, город
  - Упрощенная регистрация без сбора данных о хайде и родах
- 🔢 **Расчет количества пропущенных намазов** (несколько методов)
  - **Для мужчин:**
    - Расчет от даты совершеннолетия (первая поллюция) до начала регулярных намазов
    - Ручной ввод общего количества дней
    - Индивидуальный ввод количества для каждого типа намаза
  - **Для женщин:**
    - Расчет от даты совершеннолетия (первые месячные) с учетом:
      - Регулярности цикла и продолжительности хайда
      - Данных о родах (дата зачатия, дата родов, продолжительность нифаса)
      - Данных о выкидышах сформировавшегося плода
      - Наличия менопаузы
    - Упрощенный расчет с вводом количества дней вручную
    - Индивидуальный ввод количества для каждого типа намаза
- ➕ **Отслеживание восполненных намазов**
  - Компактный интерфейс с разделением на категории
  - Быстрые действия: ±1 намаз
  - Точная настройка: ±1, ±5, ±10 намазов
  - Ручной ввод восполненного количества
  - История изменений
  - Отдельные категории: обычные и сафар намазы
- 📿 **Управление постами**
  - Расчет по количеству пропущенных лет
  - Ручной ввод количества дней
  - Отслеживание восполненных дней
  - Быстрые действия для отметки
- 📊 **Детальная статистика и прогресс**
  - Краткая сводка с прогресс-барами
  - Детальная разбивка по типам намазов
  - История последних действий
  - Автоматическое обновление
- ⚙️ **Гибкие настройки профиля**
  - Изменение базовых данных (пол, дата рождения, город)
  - Управление ежедневными уведомлениями (вкл/выкл)
  - Полный экспорт данных со статистикой
  - Полный сброс с подтверждением
- 🌙 **Система уведомлений**
  - Вечерние мотивационные напоминания (17:00)
  - Ночные напоминания со статистикой (19:00)
  - Возможность отключения через настройки
  - Случайные мотивирующие тексты и аяты

### Для модераторов:
- 📈 **Общая статистика** по всем пользователям
  - Статистика по полу и возрасту
  - Топ-5 городов
  - Общий прогресс восполнения
- 📢 **Гибкая система рассылок** с фильтрами:
  - По полу (мужчины/женщины)
  - По возрастным группам (до 18, 18-24, 25-34, 35-44, 45-54, 55+)
  - По городам
  - Комбинированные фильтры
  - Предпросмотр перед отправкой

### Для администраторов:
- 👥 **Управление администраторами и модераторами**
  - Назначение и снятие прав
  - Просмотр списка админов и модераторов
  - Безопасность: нельзя лишить прав самого себя
- Весь функционал модераторов и пользователей

## 🔧 Технический стек

- **Backend**: Python 3.11+
- **Telegram Bot Framework**: aiogram 3.2.0
- **База данных**: SQLite с aiosqlite
- **Планировщик задач**: APScheduler 3.10.4
- **Контейнеризация**: Docker

## 📦 Установка и запуск

### Вариант 1: Docker (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository_url>
cd yashel_tracker
```

2. Создайте файл `.env` в корневой директории:
```env
BOT_TOKEN=your_telegram_bot_token
ADMIN_IDS=123456789,987654321
DATABASE_URL=sqlite:///data/yashel_tracker.db
DEBUG=False
```

3. Запустите через Docker Compose:
```bash
cd docker
docker-compose up -d
```

### Вариант 2: Локальный запуск

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` (как в варианте 1)

3. При необходимости выполните миграцию БД:
```bash
python migrations/migrate_v2.py
```

4. Запустите приложение:
```bash
python main.py
```

## 📁 Структура проекта

```
yashel_tracker/
├── app/
│   ├── bot/                    # Логика Telegram бота
│   │   ├── handlers/           # Обработчики сообщений
│   │   │   ├── common/         # Общие команды (start, help, cancel)
│   │   │   ├── user/           # Пользовательские функции
│   │   │   │   ├── registration.py      # Регистрация пользователей
│   │   │   │   ├── prayer_calculation.py # Расчет намазов
│   │   │   │   ├── prayer_tracking.py   # Отслеживание намазов
│   │   │   │   ├── fasting.py           # Управление постами
│   │   │   │   ├── statistics.py        # Статистика пользователя
│   │   │   │   └── settings.py          # Настройки профиля
│   │   │   ├── moderator/      # Функции модераторов
│   │   │   │   ├── broadcast.py         # Система рассылок
│   │   │   │   └── user_statistics.py   # Общая статистика
│   │   │   └── admin/          # Административные функции
│   │   │       └── admin_management.py  # Управление правами
│   │   ├── keyboards/          # Inline и reply клавиатуры
│   │   ├── states/             # Состояния FSM
│   │   ├── filters/            # Фильтры (роли пользователей)
│   │   ├── middlewares/        # Промежуточное ПО
│   │   └── utils/              # Утилиты бота
│   ├── core/                   # Основная логика
│   │   ├── database/           # Работа с БД
│   │   │   ├── models/         # Модели данных
│   │   │   └── repositories/   # Репозитории
│   │   ├── services/           # Бизнес-логика
│   │   │   ├── calculation_service.py        # Расчет намазов
│   │   │   ├── fasting_calculation_service.py # Расчет постов
│   │   │   ├── prayer_service.py             # Управление намазами
│   │   │   ├── user_service.py               # Управление пользователями
│   │   │   ├── broadcast_service.py          # Рассылки
│   │   │   └── statistics_service.py         # Статистика
│   │   └── config.py           # Конфигурация
│   └── tasks/                  # Фоновые задачи
│       ├── daily_notifications.py # Ежедневные уведомления
│       └── scheduler.py           # Планировщик
├── docker/                     # Docker конфигурация
├── migrations/                 # SQL миграции
│   ├── create_tables.sql       # Создание таблиц
│   └── migrate_v2.py           # Миграция структуры БД
└── main.py                     # Точка входа
```

## 🕌 Типы намазов

Бот поддерживает отслеживание следующих типов намазов:
- **Обязательные**: Фаджр, Зухр, Аср, Магриб, Иша
- **Витр**
- **Сафар**: Зухр сафар, Аср сафар, Иша сафар (сокращенные в путешествии)

## 📊 Методы расчета намазов

### Для мужчин:

1. **От даты совершеннолетия**: Расчет от даты первой поллюции/эякуляции до начала регулярных намазов
2. **Ручной ввод общего количества**: Прямое указание количества пропущенных дней
3. **Индивидуальный ввод**: Указание количества для каждого типа намаза отдельно

### Для женщин:

1. **Детальный расчет с учетом физиологии**:
   - Дата совершеннолетия (первые месячные)
   - Регулярность цикла и средняя продолжительность хайда
   - **Роды**: дата зачатия, дата родов, продолжительность нифаса, изменение хайда после родов
   - **Выкидыши сформировавшегося плода**: дата зачатия, дата выкидыша, продолжительность нифаса
   - **Менопауза**: если была раньше начала намазов
   - Бот автоматически вычитает периоды хайда, нифаса и беременности

2. **Упрощенный расчет**: 
   - Ручной ввод количества пропущенных дней
   - Индивидуальный ввод для каждого типа намаза

### Важные особенности расчета для женщин

**По ханафитскому мазхабу:**
- Минимальная продолжительность хайда: 3 дня (72 часа)
- Максимальная продолжительность хайда: 10 дней (240 часов)
- Максимальная продолжительность нифаса: 40 дней (960 часов)
- Между хайдами должно пройти минимум 15 дней (360 часов)

**Расчет включает:**
- Автоматическое вычитание дней хайда из общего периода
- Учет изменения продолжительности хайда после родов
- Полное исключение периодов беременности и нифаса
- Добавление дней от менопаузы до начала намазов (если менопауза была раньше)
- Надбавка 1% для подстраховки (лучше восполнить больше)

## 📿 Расчет постов

### Методы расчета:

1. **По количеству лет**: Указание количества пропущенных лет (30 дней на год)
2. **Ручной ввод**: Прямое указание количества пропущенных дней

**Важно**: В текущей версии для упрощения не учитываются дни хайда и нифаса. Посты восполняются за все пропущенные дни.

## ⏰ Автоматические уведомления

- **Вечерние мотивационные напоминания**: В 17:00 (настраивается в `scheduler.py`)
  - Случайные мотивирующие тексты
  - Аяты из Корана о намазе
  - Хадисы о важности намаза

- **Ночные напоминания со статистикой**: В 19:00
  - Персональная статистика
  - Количество оставшихся намазов
  - Только для пользователей с непогашенными долгами

- **Управление уведомлениями**:
  - Включены по умолчанию при регистрации
  - Можно отключить через ⚙️ Настройки
  - Административные рассылки приходят всегда

## 🔐 Система ролей

- **Пользователь**: Основной функционал отслеживания намазов и постов
- **Модератор**: + общая статистика и система рассылок с фильтрами
- **Администратор**: + управление ролями и правами пользователей

## 🗃️ База данных

### Основные таблицы:

- `users` - профили пользователей с расширенными данными
  - Базовая информация (telegram_id, username, gender, birth_date, city)
  - Данные о намазах (prayer_start_date, adult_date)
  - Данные о постах (fasting_missed_days, fasting_completed_days)
  - Данные для женщин (hayd_average_days, childbirth_count, childbirth_data)
  - Настройки (role, is_registered, daily_notifications_enabled)
  
- `prayers` - счетчики намазов по типам (user_id, prayer_type, total_missed, completed)
- `prayer_history` - детальная история всех изменений
- `admins` - система администраторов и модераторов

### Миграции:

При обновлении структуры БД используйте:
```bash
python migrations/migrate_v2.py
```

Скрипт миграции:
- Создает автоматическую резервную копию
- Безопасно обновляет структуру таблиц
- Сохраняет все существующие данные
- При ошибке восстанавливает из бэкапа

## 🚀 Архитектура и масштабируемость

### Текущая реализация:

**Паттерны проектирования:**
- Repository Pattern для работы с данными
- Service Layer для бизнес-логики
- FSM (Finite State Machine) для управления диалогами
- Dependency Injection через конструкторы

**Ключевые сервисы:**
- `CalculationService` - сложная логика расчета намазов для обоих полов
- `FastingCalculationService` - расчет пропущенных постов
- `PrayerService` - управление намазами и статистикой
- `BroadcastService` - система рассылок с фильтрацией
- `UserService` - управление пользователями

**Особенности:**
- Асинхронная архитектура (asyncio, aiosqlite)
- Разделение обязанностей (handlers → services → repositories)
- Централизованная конфигурация
- Структурированное логирование

### Планируемые улучшения:

- 📍 **Интеграция времен намазов**
  - API для получения точных времен намазов по GPS
  - Напоминания перед временем намаза
  - Учет местоположения пользователя

- 📖 **Модуль чтения Корана**
  - Ежедневные аяты с переводом
  - Система изучения сур
  - Прогресс чтения

- 🏆 **Геймификация и достижения**
  - Система рейтингов
  - Соревнования между пользователями
  - Мотивационные достижения и бейджи

- 🌐 **Web-интерфейс**
  - Dashboard для детальной статистики
  - Административная панель
  - Экспорт данных в различных форматах

- 📊 **Расширенная аналитика**
  - Графики прогресса
  - Прогнозирование завершения
  - Анализ активности

## 🤝 Участие в разработке

1. Форкните проект
2. Создайте ветку для новой функции:
```bash
git checkout -b feature/amazing-feature
```

3. Следуйте архитектуре проекта:
   - Модели в `app/core/database/models/`
   - Бизнес-логика в `app/core/services/`
   - Обработчики в соответствующих папках `app/bot/handlers/`
   - Используйте type hints и docstrings
   - Следуйте PEP 8

4. Добавьте тесты при необходимости

5. Зафиксируйте изменения:
```bash
git commit -m 'Add: amazing feature'
```

6. Отправьте в ветку:
```bash
git push origin feature/amazing-feature
```

7. Откройте Pull Request

## 🐛 Отладка и логирование

- Уровень логирования настраивается через `DEBUG` в `.env`
- Логи выводятся в консоль (Docker logs доступны через `docker logs`)
- Логи сохраняются в `logs/` при использовании Docker
- Для отладки БД можно использовать SQLite браузер (DB Browser for SQLite)

**Полезные команды Docker:**
```bash
# Просмотр логов
docker logs yashel_tracker_bot -f

# Перезапуск контейнера
docker-compose restart

# Остановка
docker-compose down

# Полная пересборка
docker-compose up -d --build
```

## 📝 Конфигурация

Основные настройки в `app/core/config.py`:

```python
# Возраст совершеннолетия
ADULT_AGE_MALE = 12        # Лунных лет
ADULT_AGE_FEMALE = 9       # Лунных лет

# Ограничения для женщин (по ханафитскому мазхабу)
HAYD_MIN_DAYS = 3
HAYD_MAX_DAYS = 10
NIFAS_MAX_DAYS = 40

# Время напоминаний (час в 24-часовом формате)
DAILY_REMINDER_HOUR = 17   # Вечерние напоминания
```

Настройка времени уведомлений в `app/tasks/scheduler.py`:
```python
# Вечерние мотивационные напоминания
CronTrigger(hour=17, minute=0, second=0)

# Ночная статистика
CronTrigger(hour=19, minute=0, second=0)
```

## 📋 Требования к системе

**Минимальные:**
- Python 3.11+
- 100MB RAM
- 50MB свободного места (для SQLite БД)
- Стабильное интернет-соединение для Telegram API

**Рекомендуемые (Docker):**
- Docker 20.10+
- Docker Compose 2.0+
- 256MB RAM
- 100MB свободного места

## 🔒 Безопасность

- ✅ Токен бота хранится в переменных окружения
- ✅ ID администраторов проверяются при каждом запросе
- ✅ Автоматическое создание резервных копий БД при миграциях
- ✅ Валидация всех пользовательских данных
- ✅ Защита от SQL-инъекций через параметризованные запросы
- ✅ FSM для контроля состояний и предотвращения некорректных действий
- ✅ Нельзя лишить прав самого себя (для админов)
- ✅ История всех изменений в prayer_history

## 📈 Производительность

- ⚡ Асинхронная архитектура с aiosqlite
- ⚡ Индексы БД для оптимизации запросов
- ⚡ Эффективная фильтрация пользователей для рассылок
- ⚡ Batch-обработка при массовых операциях
- ⚡ MemoryStorage для FSM (быстрый доступ к состояниям)

**Метрики:**
- Время отклика бота: < 200ms (при нормальной нагрузке)
- Рассылка для 1000 пользователей: ~3-5 минут
- Размер БД: ~1KB на пользователя (средний)

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для деталей.

## 🤲 Дуа

Барака-Ллаху фикум! Пусть этот бот поможет верующим в восполнении намазов и приближении к Всевышнему Аллаху.

---

**🔧 Техническая поддержка**: Создайте Issue в репозитории  
**💬 Обратная связь**: Используйте Discussions для предложений  
**📧 Контакты**: [@timer_hub](https://t.me/timer_hub)

**🎯 Цель проекта**: Облегчить мусульманам процесс восполнения пропущенных намазов через удобный и функциональный инструмент.

---

## 📚 Дополнительные ресурсы

- [Документация aiogram 3.x](https://docs.aiogram.dev/en/latest/)
- [APScheduler документация](https://apscheduler.readthedocs.io/)
- [SQLite документация](https://www.sqlite.org/docs.html)
- [Python asyncio](https://docs.python.org/3/library/asyncio.html)

## 🔄 История версий

**v1.0.0** (Текущая версия)
- ✅ Базовая функциональность расчета и отслеживания намазов
- ✅ Система ролей (пользователь, модератор, администратор)
- ✅ Рассылки с фильтрами
- ✅ Управление постами
- ✅ Настраиваемые уведомления
- ✅ Детальный расчет для женщин с учетом физиологии
- ✅ Статистика и история изменений
- ✅ Docker поддержка